/**
 * Application session type — re-exported from lib/auth.ts for convenient imports in Client Components.
 * Server Components should import directly from @/lib/auth.
 */
export type { AppSession } from "@/lib/auth";
